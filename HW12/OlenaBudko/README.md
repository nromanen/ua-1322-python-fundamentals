## Install
`pip install -r requirements.txt`

## Run
`flask run -h 0.0.0.0`

### Info
`default username and password is admin/admin but can be changed in app.py in line 13/14`
1) app uses SQLite as a database
2) Flask package SQLAlchemy
3) Implemented ORM to work with database
4) not uses a Flask-WTF
